# split string into a list of substrings

original_string = "the original string has six words"
other_string = "other/ string has slash/ characters"

# the default separator is ' '
print(original_string.split())

# define the separator as '/'
print(other_string.split('/'))